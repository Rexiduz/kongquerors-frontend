import styled from 'styled-components'
import {
  space,
  color,
  layout,
  typography,
  position,
  border,
  flexbox,
  background
} from 'styled-system'

const Box = styled.div`
  ${space};
  ${color};
  ${layout};
  ${typography};
  ${position};
  ${border};
  ${flexbox};
  ${background};
`

export default Box
